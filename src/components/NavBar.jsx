import React from 'react';
import '../styles/NavBar.css';

export default function NavBar() {
    return (
        <nav>
            <div className="links">
                <a href="#">About</a>
                <a href="#">Projects</a>
                <a href="#">Contact Me</a>
            </div>
        </nav>
    );
}
